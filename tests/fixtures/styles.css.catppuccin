/* GLOBAL CONFIGURATIONS */
* {
  background-color: transparent;
  padding: 0;
  margin: 0;
}

/* CPU */
.cpu-widget {
  background-color: rgba(243, 139, 168, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
  width: 90px;
  min-width: 90px;
  max-width: 90px;
}
.cpu-widget .label {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  padding: 2px 0 0 0;
  width: 50px;
  min-width: 50px;
  max-width: 50px;
}
.cpu-widget .icon {
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  font-size: 16px;
  padding: 0 4px 0 0;
  color: #1e1e2e;
}

/* MEMORY */
.memory-widget {
  background-color: rgba(203, 166, 247, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
}
.memory-widget .label {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  padding: 2px 12px 0 2px;
}
.memory-widget .icon {
  font-family: "0xProto Nerd Font";
  color: #1e1e2e;
  font-size: 16px;
  font-weight: bold;
  padding-right: 12px;
  padding-left: 12px;
}

/* GPU */
.gpu-widget {
  background-color: rgba(250, 179, 135, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
}
.gpu-widget .widget-container .label,
.gpu-widget .widget-container .label.alt {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  padding: 2px 12px 0 2px;
}
.gpu-widget .widget-container .icon {
  font-family: "0xProto Nerd Font";
  font-size: 16px;
  font-weight: bold;
  color: #1e1e2e;
  padding-right: 12px;
  padding-left: 12px;
}

/* DISK */
.disk-widget {
  background-color: rgba(137, 220, 235, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
}
.disk-widget .widget-container .label,
.disk-widget .widget-container .label.alt {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  padding: 2px 12px 0 2px;
}
.disk-widget .widget-container .icon {
  font-family: "0xProto Nerd Font";
  font-size: 16px;
  font-weight: bold;
  color: #1e1e2e;
  padding-right: 12px;
  padding-left: 12px;
}
.disk-widget .widget-container .label.status-low,
.disk-widget .widget-container .label.status-medium,
.disk-widget .widget-container .label.status-high,
.disk-widget .widget-container .label.status-critical {
  color: #1e1e2e;
}

/* GLAZEWM TILING DIRECTION */
.glazewm-tiling-direction {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
}
.glazewm-tiling-direction .btn {
  font-family: "0xProto Nerd Font";
  font-size: 14px;
  font-weight: bold;
  color: #cdd6f4;
  background: transparent;
  border: none;
  padding: 0 8px 0 8px;
  outline: none;
}
.glazewm-tiling-direction .btn:hover {
  color: #cba6f7;
}

/* GLAZEWM BINDING MODE */
.glazewm-binding-mode {
  background-color: rgba(249, 226, 175, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
}
.glazewm-binding-mode .label {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  padding: 2px 10px 0 4px;
}
.glazewm-binding-mode .icon {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  padding-left: 10px;
}

/* TRAFFIC */
.traffic-widget {
  width: 220px;
  min-width: 220px;
  max-width: 220px;
  outline: none;
}
.traffic-widget .download {
  background-color: rgba(166, 227, 161, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  color: #1e1e2e;
  padding-top: 2px;
  margin-left: 1px;
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  font-size: 12px;
}
.traffic-widget .upload {
  background-color: rgba(249, 226, 175, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  color: #1e1e2e;
  padding-top: 2px;
  margin-left: 1px;
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  font-size: 12px;
}

/* TRAFFIC MENU */
.traffic-menu {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  color: #cdd6f4;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  min-width: 220px;
}
.traffic-menu .header {
  border-bottom: 1px solid rgba(205, 214, 244, 0.2);
  background-color: rgba(24, 24, 37, 0.4);
}
.traffic-menu .header .title {
  padding-left: 6px;
  font-size: 12px;
  font-weight: bold;
  color: #cdd6f4;
  font-family: "0xProto Nerd Font";
}
.traffic-menu .header .reset-button {
  font-size: 12px;
  padding-right: 4px;
  padding-top: 2px;
  margin-right: 8px;
  font-weight: bold;
  background-color: transparent;
  font-family: "0xProto Nerd Font";
  color: #cdd6f4;
  outline: none;
}
.traffic-menu .download-speed,
.traffic-menu .upload-speed {
  font-family: "0xProto Nerd Font";
  background-color: transparent;
  margin: 16px 12px -8px 12px;
}
.traffic-menu .speed-separator {
  max-width: 1px;
  background-color: #585b70;
  margin: 32px 0 16px 0;
}
.traffic-menu .upload-speed-value,
.traffic-menu .download-speed-value {
  color: #cdd6f4;
  font-size: 24px;
  font-weight: bold;
  font-family: "0xProto Nerd Font";
}
.traffic-menu .upload-speed-unit,
.traffic-menu .download-speed-unit {
  font-family: "0xProto Nerd Font";
  color: #cdd6f4;
  font-size: 12px;
  font-weight: bold;
  padding-top: 4px;
}
.traffic-menu .download-speed-placeholder {
  color: transparent;
}
.traffic-menu .upload-speed-placeholder {
  color: transparent;
}
.traffic-menu .section-title {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  background-color: transparent;
  padding-top: 3px;
  padding-bottom: 3px;
  color: #9399b2;
  margin-bottom: 4px;
}
.traffic-menu .session-section,
.traffic-menu .today-section,
.traffic-menu .alltime-section {
  margin: 8px 8px 0 8px;
  background-color: transparent;
}
.traffic-menu .data-text,
.traffic-menu .data-value {
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  font-size: 12px;
  color: #cdd6f4;
  padding: 2px 0;
}
.traffic-menu .interface-info,
.traffic-menu .internet-info {
  background-color: rgba(24, 24, 37, 0.4);
  border-top: 1px solid rgba(205, 214, 244, 0.2);
  font-size: 12px;
  font-weight: bold;
  font-family: "0xProto Nerd Font";
  color: #89dceb;
  padding: 8px 0;
  margin-top: 16px;
}
.traffic-menu .internet-info.connected {
  font-family: "0xProto Nerd Font";
  color: #a6e3a1;
  margin-top: 0;
}
.traffic-menu .internet-info.disconnected {
  font-family: "0xProto Nerd Font";
  color: #eba0ac;
  margin-top: 0;
}

/* KOMOREBI LAYOUT */
.komorebi-active-layout {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  width: 30px;
  min-width: 30px;
  max-width: 30px;
}
.komorebi-active-layout .label {
  color: #cdd6f4;
  font-family: "0xProto Nerd Font";
  font-size: 16px;
  font-weight: bold;
  padding: 0 4px 0 0;
  outline: none;
}
.komorebi-layout-menu {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
}
.komorebi-layout-menu .menu-item {
  padding: 4px 16px;
  font-size: 12px;
}
.komorebi-layout-menu .menu-item-icon {
  font-size: 16px;
  font-weight: bold;
  color: #cdd6f4;
}
.komorebi-layout-menu .menu-item-text {
  font-family: "0xProto Nerd Font";
  padding-left: 4px;
  color: #cdd6f4;
  font-size: 12px;
  font-weight: bold;
}
.komorebi-layout-menu .menu-item:hover {
  background-color: rgba(205, 214, 244, 0.2);
}
.komorebi-layout-menu .separator {
  max-height: 1px;
  background-color: #585b70;
}

/* KOMOREBI WORKSPACES */
.komorebi-workspaces .ws-btn {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  padding: 8px 10px 6px 10px;
  outline: none;
}
.komorebi-workspaces .ws-btn.populated {
  border: 1px solid #313244;
  color: #cdd6f4;
}
.komorebi-workspaces .ws-btn.active {
  background-color: rgba(205, 214, 244, 0.8);
  border: 1px solid #1e1e2e;
  color: #1e1e2e;
}

/* MEDIA */
.media-widget {
  background-color: rgba(245, 194, 231, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  padding: 0 12px 0 12px;
  outline: none;
}
.media-widget .label {
  color: #1e1e2e;
  background: transparent;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  padding-left: 10px;
  padding-top: 2px;
}
.media-widget .btn {
  background: transparent;
  font-family: "Segoe Fluent Icons";
  color: #1e1e2e;
  font-weight: bold;
  font-size: 12px;
  padding: 3px 6px 2px 3px;
}
.media-widget .btn.play {
  font-size: 12px;
}

/* MEDIA POPUP */
.media-menu {
  min-width: 340px;
  max-width: 340px;
  background-color: rgba(17, 17, 27, 0.98);
  border: 1px solid #313244;
  border-radius: 8px;
  outline: none;
}
.media-menu .thumbnail {
  border: 1px solid #45475a;
  border-radius: 6px;
}
.media-menu .title,
.media-menu .artist,
.media-menu .source {
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  margin-left: 6px;
}
.media-menu .title {
  font-family: "0xProto Nerd Font";
  padding-left: 2px;
  font-size: 13px;
  color: #cdd6f4;
}
.media-menu .artist {
  font-family: "0xProto Nerd Font";
  font-size: 11px;
  margin-top: 2px;
  margin-bottom: 4px;
  padding-left: 2px;
  color: #9399b2;
}
.media-menu .source {
  font-family: "0xProto Nerd Font";
  font-size: 10px;
  font-weight: bold;
  color: #1e1e2e;
  background-color: #f5c2e7;
  border-radius: 4px;
  padding: 1px 5px;
}
.media-menu .source.brave,
.media-menu .source.firefox {
  background-color: #fab387;
  color: #1e1e2e;
}
.media-menu .source.opera,
.media-menu .source.vivaldi {
  background-color: #f38ba8;
  color: #1e1e2e;
}
.media-menu .source.spotify {
  background-color: #a6e3a1;
  color: #1e1e2e;
}
.media-menu .source.edge,
.media-menu .source.windows-media {
  background-color: #89b4fa;
  color: #1e1e2e;
}
.media-menu .btn {
  font-family: "Segoe Fluent Icons";
  font-size: 13px;
  font-weight: bold;
  min-width: 30px;
  max-width: 30px;
  min-height: 30px;
  max-height: 30px;
  border-radius: 4px;
  color: #cdd6f4;
}
.media-menu .btn:hover {
  background-color: rgba(205, 214, 244, 0.15);
  color: #f5c2e7;
}
.media-menu .btn.prev {
  margin-left: 2px;
}
.media-menu .btn.play {
  background-color: transparent;
  font-size: 18px;
  color: #f5c2e7;
}
.media-menu .btn.disabled {
  color: rgba(205, 214, 244, 0.3);
}
.media-menu .playback-time {
  font-size: 11px;
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  color: #9399b2;
  margin-top: 4px;
  min-width: 80px;
}
.media-menu .progress-slider {
  height: 10px;
}
.media-menu .progress-slider::groove {
  background: transparent;
  height: 4px;
}
.media-menu .progress-slider::groove:hover {
  background: rgba(205, 214, 244, 0.2);
  height: 6px;
}
.media-menu .progress-slider::sub-page {
  background: #f5c2e7;
  height: 4px;
}

/* TOOLTIP */
.tooltip {
  background-color: #11111b;
  border: 1px solid #313244;
  border-radius: 4px;
  color: #cdd6f4;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  padding: 6px 10px;
  margin-top: 4px;
}
.dark.tooltip {
  background-color: #11111b;
  border: 1px solid #313244;
  color: #cdd6f4;
}

/* SYSTRAY */
.systray {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  padding: 0 4px 0 4px;
}
.systray .widget-container {
  background: transparent;
}
.systray .pinned-container {
  background: transparent;
  padding: 0 2px 0 2px;
}
.systray .unpinned-container {
  background: transparent;
}
.systray .button {
  border-radius: 4px;
  padding: 2px;
  margin: 0 1px 0 1px;
  outline: none;
}
.systray .button:hover {
  background-color: rgba(205, 214, 244, 0.2);
}
.systray .button.dragging {
  background-color: rgba(203, 166, 247, 0.4);
}
.systray .button.drag-over,
.systray .pinned-container.drop-target {
  background-color: rgba(203, 166, 247, 0.25);
  border-radius: 4px;
}
.systray .unpinned-visibility-btn {
  color: #cdd6f4;
  font-family: "Segoe Fluent Icons";
  font-weight: bold;
  font-size: 12px;
  border-radius: 4px;
  padding: 2px 4px;
  margin-right: 2px;
  outline: none;
}
.systray .unpinned-visibility-btn:hover {
  background-color: rgba(205, 214, 244, 0.2);
}
.systray .unpinned-visibility-btn:checked {
  color: #cba6f7;
}

/* SYSTRAY POPUP */
.systray-popup {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 8px;
}
.systray-popup-container {
  background: transparent;
  padding: 8px;
}
.systray-popup .button {
  border-radius: 4px;
  padding: 6px;
  margin: 2px;
  outline: none;
}
.systray-popup .button:hover {
  background-color: rgba(205, 214, 244, 0.2);
}
.systray-popup .button.dragging {
  background-color: rgba(203, 166, 247, 0.4);
}
.systray-popup .button.drag-over {
  background-color: rgba(203, 166, 247, 0.25);
}

/* WEATHER */
.weather-widget {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
}
.weather-widget .label,
.weather-widget .label.alt {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #cdd6f4;
  padding: 2px 14px 0 12px;
}
.weather-widget .icon {
  font-family: "Segoe Fluent Icons";
  font-size: 18px;
  color: #cdd6f4;
  padding-left: 14px;
}

/* WEATHER CARD */
.weather-card {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  padding: 10px;
  outline: none;
}
.weather-card-today {
  background-color: rgba(17, 17, 27, 0.8);
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  padding: 10px;
}
.weather-card-today .label {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #cdd6f4;
}
.weather-card-today .label.location {
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  font-size: 24px;
}
.weather-card-today .label.alert {
  padding: 5px 0;
}
.weather-card-day {
  background-color: rgba(17, 17, 27, 0.8);
  font-family: "0xProto Nerd Font";
  font-weight: bold;
}
.weather-card-day .label {
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  font-size: 12px;
  color: #cdd6f4;
}

/* CLOCK + DATE (one widget, two pills, one calendar) */
.clock-widget {
  background: transparent;
  border: none;
  outline: none;
}
.clock-widget .label,
.clock-widget .label.alt {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  color: #cdd6f4;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  padding: 2px 10px 0 10px;
  margin-left: 1px;
}
.clock-widget .sep {
  background: transparent;
  border: none;
  padding: 0;
  margin: 0;
  max-width: 0;
}

/* GLAZEWM WORKSPACES */
.glazewm-workspaces {
  background: transparent;
  margin-left: 1px;
}
.glazewm-workspaces .ws-btn {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  color: #6c7086;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  padding: 8px 10px 6px 10px;
  margin-left: 1px;
  outline: none;
}
.glazewm-workspaces .ws-btn:hover {
  background-color: rgba(49, 50, 68, 0.9);
  color: #cdd6f4;
}
.glazewm-workspaces .ws-btn.empty {
  color: #6c7086;
}
.glazewm-workspaces .ws-btn.populated {
  color: #cdd6f4;
  border: 1px solid #313244;
}
.glazewm-workspaces .ws-btn.active_populated,
.glazewm-workspaces .ws-btn.active_empty {
  color: #cba6f7;
  border: 1px solid #45475a;
}
.glazewm-workspaces .ws-btn.focused_populated,
.glazewm-workspaces .ws-btn.focused_empty {
  background-color: rgba(205, 214, 244, 0.8);
  border: 1px solid #1e1e2e;
  color: #1e1e2e;
}
.glazewm-workspaces .offline-status {
  color: #6c7086;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  padding: 2px 8px 0 8px;
}

/* ACTIVE WINDOW */
.active-window-widget {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  padding: 0 10px 0 8px;
  outline: none;
}
.active-window-widget .widget-container .label,
.active-window-widget .widget-container .label.alt {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #89b4fa;
  padding: 2px 2px 0 2px;
}
.active-window-widget .widget-container .icon {
  padding: 0 4px 0 0;
}

/* CALENDAR */
.calendar {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
}
.calendar .calendar-table,
.calendar .calendar-table::item {
  background-color: rgba(24, 24, 37, 0);
  color: #cdd6f4;
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  font-size: 12px;
  margin: 0;
  padding: 0;
  outline: none;
}
.calendar .calendar-table::item:selected {
  background-color: #cdd6f4;
  border-radius: 4px;
  color: #1e1e2e;
  outline: none;
}
.calendar .day-label,
.calendar .month-label,
.calendar .date-label {
  font-family: "0xProto Nerd Font";
  font-weight: bold;
  color: #cdd6f4;
  min-width: 140px;
  max-width: 140px;
}
.calendar .day-label {
  font-size: 14px;
  margin-top: 20px;
}
.calendar .month-label {
  font-size: 12px;
}
.calendar .date-label {
  font-size: 88px;
  margin-top: -10px;
}

/* VOLUME */
.volume-widget {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  padding: 0 8px 0 8px;
  margin-left: 1px;
  outline: none;
}
.volume-widget .icon {
  font-family: "Segoe Fluent Icons";
  color: #cdd6f4;
  font-size: 16px;
  font-weight: bold;
  padding: 0x 8px 0 4px;
}
.volume-widget .label {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #cdd6f4;
  padding: 1px 4px 0 4px;
}

/* VOLUME POPUP */
.audio-menu {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  outline: none;
}
.audio-container .device {
  font-family: "0xProto Nerd Font";
  color: #cdd6f4;
  font-weight: bold;
  padding: 6px 8px 6px 4px;
  font-size: 12px;
  outline: none;
}
.audio-container .device.selected {
  background-color: rgba(205, 214, 244, 0.8);
  border-radius: 4px;
  color: #1e1e2e;
}

/* QUICK LAUNCH */
.quick-launch-widget {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  padding: 0 10px 0 10px;
  outline: none;
}
.quick-launch-widget:hover {
  background-color: rgba(49, 50, 68, 0.9);
}
.quick-launch-widget .icon {
  font-family: "0xProto Nerd Font";
  font-size: 14px;
  font-weight: bold;
  color: #89b4fa;
  padding: 1px 0 0 0;
}

/* QUICK LAUNCH POPUP
   Acrylic recipe: the window supplies the blur (popup.blur: true in config), and
   .container paints the tint on top of it. The tint MUST live on .container -
   that is the surface that actually paints; a colour on .quick-launch-popup alone
   is invisible, which is why the blurred wallpaper used to bleed through the text.
   Raise the alpha below if you still lose contrast on very bright wallpapers. */
.quick-launch-popup {
  background: transparent;
  border: 1px solid #cba6f7;
  border-radius: 8px;
  outline: none;
}
.quick-launch-popup .container {
  background-color: rgba(17, 17, 27, 0.88);
  border-radius: 8px;
}

/* QUICK LAUNCH SEARCH BAR */
.quick-launch-popup .search {
  background-color: rgba(30, 30, 46, 0.95);
  border-bottom: 1px solid rgba(205, 214, 244, 0.2);
  padding: 4px 12px;
}
.quick-launch-popup .search .search-icon {
  font-family: "0xProto Nerd Font";
  font-size: 18px;
  font-weight: bold;
  color: #89b4fa;
  padding: 0 12px 0 2px;
}
.quick-launch-popup .search .search-input {
  font-family: "0xProto Nerd Font";
  font-size: 18px;
  font-weight: bold;
  color: #cdd6f4;
  background: transparent;
  border: none;
  padding: 10px 0 10px 0;
  selection-background-color: #cba6f7;
  selection-color: #1e1e2e;
}
.quick-launch-popup .search .prefix {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  background-color: rgba(203, 166, 247, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  padding: 2px 8px 0 8px;
  margin-right: 10px;
}
.quick-launch-popup .search .search-submit-icon {
  font-family: "0xProto Nerd Font";
  font-size: 14px;
  font-weight: bold;
  color: #6c7086;
  padding-left: 8px;
}
.quick-launch-popup .search .loader-line {
  background-color: #cba6f7;
}

/* QUICK LAUNCH RESULTS */
.quick-launch-popup .results {
  background: transparent;
}
.quick-launch-popup .results-list-view {
  background: transparent;
  font-family: "0xProto Nerd Font";
  font-size: 13px;
  font-weight: bold;
  color: #cdd6f4;
  padding: 6px;
  outline: none;
}
.quick-launch-popup .results-list-view::item {
  color: #cdd6f4;
  border-radius: 4px;
  padding: 6px 8px;
}
.quick-launch-popup .results-list-view::item:hover {
  background-color: rgba(205, 214, 244, 0.1);
}
.quick-launch-popup .results-list-view::item:selected {
  background-color: rgba(203, 166, 247, 0.35);
  color: #ffffff;
}
.quick-launch-popup .results-list-view .description {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  color: #bac2de;
}
.quick-launch-popup .results-list-view .separator {
  background-color: #313244;
}
.quick-launch-popup .results-empty {
  background: transparent;
}
.quick-launch-popup .results-empty-icon {
  font-family: "0xProto Nerd Font";
  font-size: 32px;
  color: #45475a;
}
.quick-launch-popup .results-empty-text {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #6c7086;
  padding-top: 8px;
}

/* QUICK LAUNCH PREVIEW */
.quick-launch-popup .preview {
  background-color: rgba(24, 24, 37, 0.4);
  border-left: 1px solid #313244;
  padding: 12px;
}
.quick-launch-popup .preview .preview-title {
  font-family: "0xProto Nerd Font";
  font-size: 14px;
  font-weight: bold;
  color: #cdd6f4;
}
.quick-launch-popup .preview .preview-subtitle {
  font-family: "0xProto Nerd Font";
  font-size: 11px;
  font-weight: bold;
  color: #9399b2;
  padding-top: 2px;
}
.quick-launch-popup .preview .preview-text {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  color: #cdd6f4;
}
.quick-launch-popup .preview .preview-meta {
  font-family: "0xProto Nerd Font";
  font-size: 11px;
  font-weight: bold;
  color: #89dceb;
}
.quick-launch-popup .preview .preview-image {
  border: 1px solid #313244;
  border-radius: 4px;
}
.quick-launch-popup .preview.edit .preview-line-edit,
.quick-launch-popup .preview.edit .preview-text-edit {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  color: #cdd6f4;
  padding: 6px;
  selection-background-color: #cba6f7;
  selection-color: #1e1e2e;
}
.quick-launch-popup .preview.edit .preview-line-edit:focus,
.quick-launch-popup .preview.edit .preview-text-edit:focus {
  border: 1px solid #cba6f7;
}
.quick-launch-popup .preview.edit .preview-actions {
  background: transparent;
  padding-top: 8px;
}
.quick-launch-popup .preview.edit .preview-btn {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #cdd6f4;
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  padding: 4px 12px 2px 12px;
  outline: none;
}
.quick-launch-popup .preview.edit .preview-btn:hover {
  background-color: rgba(205, 214, 244, 0.2);
}
.quick-launch-popup .preview.edit .preview-btn.save {
  color: #1e1e2e;
  background-color: rgba(166, 227, 161, 0.8);
  border: 1px solid #1e1e2e;
}
.quick-launch-popup .preview.edit .preview-btn.save:hover {
  background-color: #a6e3a1;
}

/* QUICK LAUNCH CONTEXT MENU */
.quick-launch-popup .context-menu {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #cdd6f4;
  padding: 4px;
}
.quick-launch-popup .context-menu::item {
  padding: 4px 16px;
  border-radius: 4px;
}
.quick-launch-popup .context-menu::item:selected {
  background-color: rgba(205, 214, 244, 0.2);
}

/* VESKTOP (DISCORD)
   The active (pinged) and idle chips are two separate widgets that swap places.
   Both are locked to the same width so nothing on the bar shifts when they flip. */
.vesktop-widget {
  background-color: rgba(243, 139, 168, 0.8);
  border: 1px solid #1e1e2e;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
  width: 68px;
  min-width: 68px;
  max-width: 68px;
}
.vesktop-widget .widget-container .label,
.vesktop-widget .widget-container .label.alt {
  font-family: "0xProto Nerd Font";
  font-size: 12px;
  font-weight: bold;
  color: #1e1e2e;
  padding: 2px 0 0 0;
  width: 26px;
  min-width: 26px;
  max-width: 26px;
}
.vesktop-widget .widget-container .icon {
  font-family: "0xProto Nerd Font";
  font-size: 16px;
  font-weight: bold;
  color: #1e1e2e;
  padding-left: 12px;
  padding-right: 8px;
}

/* VESKTOP IDLE - shown only when you have no pings. Same 68px as the active
   chip above, with the lone icon centred, so the swap causes no layout shift. */
.vesktop-idle-widget {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
  width: 68px;
  min-width: 68px;
  max-width: 68px;
}
.vesktop-idle-widget .widget-container .icon {
  font-family: "0xProto Nerd Font";
  font-size: 16px;
  font-weight: bold;
  color: #6c7086;
  padding-left: 25px;
  padding-right: 25px;
}

/* NOTIFICATIONS */
.notification-widget {
  background-color: rgba(17, 17, 27, 0.8);
  border: 1px solid #313244;
  border-radius: 4px;
  margin-left: 1px;
  outline: none;
}
.notification-widget .icon {
  font-family: "0xProto Nerd Font";
  color: #89dceb;
  font-size: 14px;
  font-weight: bold;
  padding-left: 10px;
  padding-right: 8px;
  padding-top: 1px;
}
.notification-widget .label {
  font-family: "0xProto Nerd Font";
  color: #89dceb;
  font-size: 12px;
  font-weight: bold;
  padding-right: 10px;
  padding-top: 2px;
}
